#!/sbin/openrc-run

depends() {
	before sshd
}

start() {
	mkdir -m 700 /root/.ssh

	if [ -f /state/id_rsa.pub ]; then
		cat /state/id_rsa.pub >>/root/.ssh/authorized_keys
		chmod 600 /root/.ssh/authorized_keys
	fi
}
